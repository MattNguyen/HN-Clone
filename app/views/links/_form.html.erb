<div class="span4">
	<div class="sidebar">
		<div class="create_entry_form">
			<%= form_for(@link) do |f|%>

				<%= f.text_field :title,:placeholder => "Title" %>
				<%= f.text_field :url, :placeholder => "URL" %>

				<%= f.submit :class => "btn submit_button" %>
			<% end -%>
		</div>
	</div>
</div>	