var obj = $("#story_<%= @story.id %>_votes");
var change = function(value){
	obj.text( parseInt( obj.text() ) + value )
}


if ('<%= params[:attitude] %>' == 'positive'){
	change(1);
} else {
	change(-1);
}
